# env-validator

Pre-flight environment variable validation for AI agents. Checks that required vars exist, validates formats (wallet addresses, URLs, API keys), and produces a pass/fail report before any operation runs. Zero dependencies, pure Node.js.

See [SKILL.md](./SKILL.md) for full documentation.
