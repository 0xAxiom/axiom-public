# Gateway Watchdog Configuration
# Copy to ~/.config/gateway-watchdog/config.env and fill in your values

# Gateway health endpoint
GATEWAY_URL="http://localhost:18789/health"

# How often to check (seconds)
CHECK_INTERVAL=60

# How many consecutive failures before alerting
FAIL_THRESHOLD=3

# Telegram alerts (get bot token from @BotFather, chat ID from @userinfobot)
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""

# Auto-restart the gateway on failure?
AUTO_RESTART=false

# Command to restart (customize for your setup)
# macOS launchd:
RESTART_CMD="launchctl kickstart -k gui/$(id -u)/com.clawdbot.axiom"
# systemd:
# RESTART_CMD="sudo systemctl restart openclaw"
# Docker:
# RESTART_CMD="docker restart openclaw"

# Log file location
LOG_FILE="$HOME/.config/gateway-watchdog/watchdog.log"

# Quiet hours (no alerts) — leave empty to disable
# QUIET_HOURS_START=23
# QUIET_HOURS_END=7
